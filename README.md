# Player Vault

```
# 1. Install dependencies
npm i
  
# 2. Install plugins referenced by your chosen template
npx quartz plugin install --from-config
 
# 3. Preview your site locally
npx quartz build --serve
```